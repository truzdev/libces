# libces
Library of c in spanish  c en español 
```bash
curl -s https://raw.githubusercontent.com/truzdev/libces/main/libces.h > $PREFIX/include/libces.h
```
### como usarlo
```c
#include <libces.h>
entero principal() {
    si (5>5) {
        // contenido
    } sino {
        // contenido
    }
   devolver 0;
}
```
